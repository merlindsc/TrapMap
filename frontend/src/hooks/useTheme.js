/* ============================================================
   TRAPMAP - USE THEME HOOK
   Export for convenience
   ============================================================ */

export { useTheme } from '../context/ThemeContext';
